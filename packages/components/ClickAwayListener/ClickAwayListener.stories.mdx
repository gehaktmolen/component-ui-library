import { Meta, ArgTypes, Canvas } from '@storybook/blocks';
import { ClickAwayListener } from './ClickAwayListener';
import * as ClickAwayListenerDefaultStories from './ClickAwayListener.stories';

<Meta title="Utils/ClickAwayListener" />

# Click-Away Listener

---

The Click-Away Listener component detects when a click event happens outside of its child element.

## Introduction

Click-Away Listener is a utility component that listens for click events outside its child. (Note that it only accepts one child element.)

This is useful for components like the [Popper](?path=/docs/utils-popper--docs) which should close when the user clicks anywhere else in the document.

Click-Away Listener also supports the [Portal](?path=/docs/utils-portal--docs) component.

## Component

### Usage

```jsx
import { ClickAwayListener } from '../ClickAwayListener';

export default function MyApp() {
    return <ClickAwayListener>{/* the child listening for a click outside its container */}</ClickAwayListener>;
}
```

### Basics

The following demo shows how to hide a menu dropdown when users click anywhere else on the page:

<Canvas sourceState="hidden" of={ClickAwayListenerDefaultStories.Basics} />

### Usage with Portal

The following demo uses the [Portal](?path=/docs/utils-portal--docs) component to render the dropdown into a new subtree outside of the current DOM hierarchy:

<Canvas sourceState="hidden" of={ClickAwayListenerDefaultStories.UsageWithPortal} />

## Customization

### Listening for leading events

By default, the Click-Away Listener component responds to trailing events—the end of a click or touch.

You can set the component to listen for leading events (the start of a click or touch) using the `mouseEvent` and `touchEvent` props, as shown in the following demo:

<Canvas sourceState="hidden" of={ClickAwayListenerDefaultStories.LeadingEvents} />

When the component is set to listen for leading events, interactions with the scrollbar are ignored.

## Accessibility

By default, Click-Away Listener will add an `onClick` handler to its child. This can result in screen readers announcing that the child is clickable, even though this `onClick` handler has no effect on the child itself.

To prevent this behavior, add `role="presentation"` to the child element:

```jsx
<ClickAwayListener>
    <div role="presentation">
        <h1>non-interactive heading</h1>
    </div>
</ClickAwayListener>
```

## API

### Import

```jsx
import { ClickAwayListener } from '../ClickAwayListener';
```

### Parameters

<ArgTypes of={ClickAwayListener} />
